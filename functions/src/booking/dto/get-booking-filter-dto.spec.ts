import { GetBookingFilterDto } from './get-booking-filter-dto';

describe('GetBookingFilterDto', () => {
  it('should be defined', () => {
    expect(new GetBookingFilterDto()).toBeDefined();
  });
});
